# OpenBrainFun Agent Guidelines

When changing files in this `openbrainfun/` tree:

1. Run `gofmt -w` on all modified Go files before commit.
2. Run `go vet ./...` in this module before commit.
3. Keep README walkthrough documentation up to date when commands or endpoints change.
